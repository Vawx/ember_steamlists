import { gameTitleInfo } from 'steamlists/helpers/game-title-info';
import { module, test } from 'qunit';

module('Unit | Helper | game title info');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = gameTitleInfo([42]);
  assert.ok(result);
});
