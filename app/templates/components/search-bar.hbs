{{yield}}

<form class="navbar-form navbar-right" role="search">
  <div class="form-group">
    {{input type=text value=gameID id="gameID" placeholder="Game name..."}}
  </div>
  <button type="submit" class="btn btn-default" {{action 'findFromID'}}>Submit</button>
</form>
