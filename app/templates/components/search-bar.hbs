{{yield}}

<form class="navbar-form navbar-right" role="search">
  <div class="form-group">
    {{input type=number value=gameID id="gameID" placeholder="Game ID..."}}
  </div>
  <button type="submit" class="btn btn-default" {{action 'gameStats'}}>Submit</button>
</form>
